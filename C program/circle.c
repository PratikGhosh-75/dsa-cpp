#include <stdio.h>
int main()
{
    float pi,radius;
    pi=3.14;
    printf("Enter the radius of circle: \n");
    scanf("%f",&radius);
    printf("Area of circle is %f\n",pi*radius*radius);

    return 0;
}